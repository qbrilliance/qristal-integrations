## VSCode resources
This directory holds all items that integrate QB SDK into a VS Code IDE environment.

The repository is set up with a devcontainer.json configuration file. To use this in VS code:
- Install the `ms-vscode-remote.remote-containers` extension
- Open the "Command Palette" and type + select "Dev Containers: Rebuild and Reopen in Container"
